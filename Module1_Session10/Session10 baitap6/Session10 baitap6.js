
let math = 8.5;
let physics = 7.0;
let chemistry = 9.0;

// Công thức: (Toán + Lý + Hóa) / 3
const average = (math + physics + chemistry) / 3;
console.log("Điểm trung bình là: " + average.toFixed(2));

// Hoặc hiển thị thông báo trên màn hình
alert("Điểm trung bình 3 môn là: " + average.toFixed(2));